export const studentProfileController = (req: any, res: any) => {
		// Student profile logic here
		res.send("Student Profile endpoint");
}

export const booksTrackingController = (req: any, res: any) => {
		// Books tracking logic here
		res.send("Books Tracking endpoint");
}